<!DOCTYPE html>
<html>
  <head>
    <title>todo list</title>
    <link rel="stylesheet" type="text/css" href="style.css"/>
  </head>
  <body>
    <div class="container">
      <%-include("header", { checkAll })  %>
      <%-include("todolist", { dataList, editId })  %>
      <%-include("footer", { unfinished, pathname, todoLength: dataList.length }) %>
    </div>  
  </body>
</html>
